'use strict';

/**
 * @file Events module exports
 * @module fuelink/events
 */

const { FuelinkEmitter, EventPriority } = require('./EventEmitter');

module.exports = {
    FuelinkEmitter,
    EventPriority
};
