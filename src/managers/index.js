'use strict';

/**
 * @file Managers module exports
 * @module fuelink/managers
 */

const { NodeManager } = require('./NodeManager');
const { PlayerManager } = require('./PlayerManager');

module.exports = {
    NodeManager,
    PlayerManager
};
