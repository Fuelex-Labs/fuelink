'use strict';

/**
 * @file Adapters module exports
 * @module fuelink/adapters
 */

const { VoiceAdapter } = require('./VoiceAdapter');
const { DistubeAdapter } = require('./DistubeAdapter');

module.exports = {
    VoiceAdapter,
    DistubeAdapter
};
