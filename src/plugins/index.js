'use strict';

/**
 * @file Plugins module exports
 * @module fuelink/plugins
 */

const { PluginManager } = require('./PluginManager');
const { BaseSource } = require('./sources/BaseSource');

module.exports = {
    PluginManager,
    BaseSource
};
